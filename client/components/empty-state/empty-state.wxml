<view class="empty-state">
  <image class="empty-image" src="{{image || '/images/default/empty.png'}}" mode="aspectFit" />
  <text class="empty-text">{{text}}</text>
  <button class="empty-btn" wx:if="{{showBtn}}" bindtap="onBtnTap">{{btnText}}</button>
</view>
